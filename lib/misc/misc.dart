import 'package:flutter/material.dart';

const String AppName = 'Shop App';
const Color pitchBlack = Colors.black;
const Color buttonCustomColor = Color(0xff7B61FF);
const buttonCustomShadow = BoxShadow(
  color: Colors.grey,
  blurRadius: 10,
  spreadRadius: 1.0,
  blurStyle: BlurStyle.outer,
  offset: Offset(1, 2),
);
